---
  el:
    hosts:
      centos7:
        ansible_connection: docker
 
  deb:
    hosts:
      ubuntu:
        ansible_connection: docker
  
  local:
    hosts:
      debian:
        ansible_connection: local
        ansible_host: localhost